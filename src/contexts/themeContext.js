// import createContext from '../lib/Context';
import React from 'react';

// const themeContext = createContext(value=null);
const themeContext = React.createContext();

export default themeContext;
